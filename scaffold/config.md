# Configuration

User preferences for this NLA. This file is read at session start and its directives are applied throughout.

Directives here are always active. For context-specific preferences, add routing rules that point to files in `config/`.

---

## Always Active

Framework path: ../nla-framework/

TK note verbosity: standard (flag significant uncertainty and structural changes).

## Contextual

When in maintenance mode, also read `config/maintenance.md`.

---

*Edit this file by running `/preferences`, or edit directly. Changes take effect at next session start or `/startup` reload.*
