# Configuration

User preferences for the NLA Framework. This file is read at session start and its directives are applied throughout.

Directives here are always active. For context-specific preferences, add routing rules that point to files in `config/`.

---

## Always Active

Verbosity: standard (explain reasoning for significant decisions, skip narration for routine operations).

## Contextual

When in maintenance mode, also read `config/maintenance.md`.

---

*Edit this file by running `/preferences`, or edit directly. Changes take effect at next session start.*
