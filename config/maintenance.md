# Maintenance Mode Preferences

These preferences apply when `/maintain` is active.

---

Always propose changes before editing — show the current text, proposed text, and rationale before making any edit.

When processing friction log entries, summarize what you plan to do before starting. Don't jump straight into edits.
